"""HTML report generation for tax declaration outputs.

Level 2: renders templates to static HTML files. Templates are Jinja2 so they
can be reused by a future FastAPI server (Level 3) without changes.
"""
